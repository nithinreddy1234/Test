# Launchpad Test App

A tiny static site used to verify Launchpad imports, builds, and preview URLs.

## Local preview

Open `index.html` in a browser, or serve the directory with any static server.

## Why this repo exists

This repository is intentionally simple so it is easy to:

- import into Launchpad
- deploy without extra setup
- confirm preview URLs are working
